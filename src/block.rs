//! Command blocks (M3 scope).
//!
//! TODO(M3): Group prompt/command/output triples into "Warp-style" blocks
//! using shell integration markers (OSC 133 / `__aiterm_prompt_start`). Blocks
//! become the unit the LLM agent operates on.
