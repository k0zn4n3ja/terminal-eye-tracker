//! LLM client + agent loop (M4 scope).
//!
//! TODO(M4): Streaming client over `reqwest` (rustls), tool-use schema, and
//! an agent loop that can read [`crate::block`] history and propose commands.
