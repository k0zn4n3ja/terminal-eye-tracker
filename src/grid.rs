//! Terminal grid model (M2 scope).
//!
//! TODO(M2): Cell + Line + Grid types with scrollback, dirty tracking, and
//! reflow on resize. Designed to feed the renderer dirty rects rather than the
//! whole screen each frame.
